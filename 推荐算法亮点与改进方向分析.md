# 推荐算法亮点与改进方向分析报告

> 分析对象：`recommendation-service`（CQUST-AIStudy 学习教练 Agent 推荐微服务）
> 分析依据：通读 12 个核心算法文件 + 配置参数 + 既有评审记录（`review-stage/AUTO_REVIEW.md`），并核对了爬虫数据来源（`LeetCodeClaw/models.go`、`schema.sql`）。
> 产出目标：识别真实亮点；在**基础模型不变**（保留 BKT / 艾宾浩斯 / TF-IDF / Embedding）前提下给出针对本项目的优化方向。

---

## 一、系统概览

整条推荐链路是一条教科书级 + 工程化的两段式（召回→排序）流水线：

```
学生侧画像                 题目侧特征                  推荐
┌──────────────┐        ┌────────────────────┐     ┌──────────────────┐
│ BKT 掌握度 P(L)│        │ Layer1 词典加权(54)  │     │ 6 路召回          │
│ 艾宾浩斯 R(t) │  ───►  │ Layer2 TF-IDF 质心  │ ──► │ 六因子排序         │ ──► Top-N
│ Wilson 置信度  │  统一   │ Layer3 语义向量(可选)│     │ MMR 多样性重排     │
│ P(recall)=P(L)×R(t)    └────────────────────┘     │ 可解释理由生成     │
└──────────────┘                                  └──────────────────┘
```

- **画像侧**回答"学生会不会、记不记得、置信度高不高"；
- **题目侧**回答"这道题考什么知识点、相关度多少"；
- **推荐侧**把两者组合，输出带自然语言理由的个性化列表。

下面分别给出**亮点**和**改进方向**。

---

## 二、核心亮点（共 7 条，均有代码支撑）

### 亮点 1：`P(recall) = P(L) × R(t)` 的统一状态建模 —— 全系统最亮的点 ⭐

**是什么**：把 BKT 的潜在掌握度 `P(L)` 和艾宾浩斯的记忆保留率 `R(t)` 乘成一个单一的"回忆概率" `P(recall)` 存库（`unified_state.py`）。

**为什么是亮点**：这不是简单拼接，而是解决了一个**真实的、很容易踩的坑**——"双重衰减"。

- 直接堆两个衰减模型，学生没练习几天后，BKT 会衰减一次、遗忘曲线又衰减一次，掌握度会被**双重衰减系统性低估**。
- 本系统用了**非累积衰减**的精巧设计（`unified_state.py:114-169`）：
  1. 衰减前，先把存储的 `P(recall)` **除以旧 R(t)**，反推出"纯粹"的 `P(L)`（`P(L) = P(recall)/R(t)`，对应 `bkt.py:63-84` 的 `recover_p_l`）；
  2. 再算**新**的 `R(t)`；
  3. 重新相乘 `P(recall) = P(L) × R_new(t)`。
- 练习后更新同理：先反推 `P(L)` → BKT 贝叶斯更新 → 重置遗忘（刚练完 `R(t)≈1`）→ `P(recall)≈P(L)`。

**学术定位**：这是把"知识追踪（KT）"和"间隔重复（SR）"两类模型**解耦存储、耦合计算**的工程化处理。"学习-遗忘联合模型"是 KT 领域的一类代表工作——**DASH**（Lindsey, Shroyer, Pashler & Mozer, 2014, *Psychological Science*）与 **DAS3H**（Choffin, Popineau, Bourda & Vie, 2019, *EDM*；arXiv:1905.06873）都将遗忘曲线与练习历史联合建模；本系统的 `P(recall)=P(L)×R(t)` 与之思路相通，但采用"衰减前先反推纯 P(L)"的简化策略。**其工程价值在于避免掌握度被遗忘因子重复扣减**：若每次定时衰减都直接在 P(recall) 上再叠一层 R(t)，掌握度会被系统性低估。多数学生项目只是把两个模型各自跑一遍，本系统做到了存储与计算的一致性。

> ⚠️ **文献勘误（仓库已修复）**：`README.md`、`app/services/ebbinghaus.py`、`app/services/unified_state.py` 原将此引用误写为"Lindsey et al. (2014). DAS3H model. CHI '14"——三处笔误：①模型误作 DAS3H（该篇实为 **MCM** 遗忘模型，属 Mozer 实验室 DASH 系列间隔重复工作）；②期刊 *Psychological Science* 误作 CHI；③**DAS3H 实属 Choffin et al. (2019)**。三处现均已更正。

---

### 亮点 2：三层混合题目-知识点匹配（词典 + TF-IDF 质心 + 语义向量）

**是什么**：把"从题目文字抽取知识点"这件事拆成三层，**逐层兜底**（`knowledge_tags.py` / `tfidf_model.py` / `embedding_model.py`）。

| 层 | 方法 | 信号强度 | 角色 |
|----|------|----------|------|
| Layer 1 | 54 知识点词典 + 中英双语同义词加权打分 | 强（标题命中直接 1.0） | 主力，离线一次性打标 |
| Layer 2 | TF-IDF 质心（Rocchio 分类思想，纯 Python） | 中 | 补 Layer 1 漏的同义/跨语言表达 |
| Layer 3 | bge-small-zh 语义向量 + 暴力余弦 | 弱但泛化 | 防漏召回（"DP"="动态规划"） |

**为什么是亮点**：

1. **强信号优先 + 弱信号兜底**的分层策略，符合工业推荐系统"multi-channel matching"思想（Davidson et al., 2010, YouTube 推荐）。
2. **TF-IDF 用纯 Python 实现**（`tfidf_model.py:46-96`），不依赖 scikit-learn，IDF 用 sklearn 的平滑公式 `log((1+N)/(1+df))+1`，向量 L2 归一化后余弦=点积——既保持算法正确性，又消除了重依赖。
3. **质心是 relevance_score 加权的**（`tfidf_model.py:158-178`），不是简单平均，让 Layer 1 的强信号能传导到 Layer 2。
4. relevance_score 从"固定 1.0"升级为"真实连续分数 [0,1]"，让后续排序的 `need_match` 加权更精细。

---

### 亮点 3：完整的工业级推荐流水线（6 步 / 6 路召回 / 六因子排序 / MMR）

**6 步流水线**（`recommendation_service.py:96-193`）：画像快照 → 反馈上下文 → 多路召回 → 排序打分 → 多样性重排 → 理由生成 + 持久化。

**6 路召回**（`recall.py`）：弱项标签(60%) + 难度进阶(25%) + 探索(15%) + 错题本(20%) + 语义近邻(15%) + 热门兜底。

**六因子排序**（`ranking.py:184-266`）：

```
score = 0.40·need_match + 0.20·difficulty_fit + 0.15·P_success
      + 0.10·novelty + 0.10·quality + 0.10·wrong_question - 0.15·repeat_penalty
```

每个因子都有清晰的设计动机。`need(tag)`（`ranking.py:53-66`）融合三信号：

```
need = 0.75·(1 − m) + 0.10·f + 0.15·w_course
```

其中 `m` = 掌握度归一化 [0,1]（mastery_score/100）、`f` = 遗忘度归一化 [0,1]（forgetting_score/100）、`w_course` = 该知识点的课程权重（来自 54 知识点词典，[0,1]）。`difficulty_fit` 用高斯衰减 `e^(−|d_target − d_problem|)` 做平滑匹配而非硬阈值。

**为什么是亮点**：这是**召回-排序两段式 + 多样性约束**的标准工业架构（区别于"一道题一道题拍脑袋推"的学生项目），且每个环节都可独立调参、独立降级。

---

### 亮点 4：Wilson 置信区间做小样本修正

**是什么**（`wilson.py`）：综合置信度 = 样本充足度(0~50) + 表现置信度(0~50)，两项都来自真实统计量。

**Wilson 下界公式**（`wilson.py:25-54`，二项比例 95% 置信区间下界，z=1.95）：

```
L = ( p + z²/(2n) − z·√( p(1−p)/n + z²/(4n²) ) ) / ( 1 + z²/n )
```

其中 `p = success/attempt`（成功率）、`n` 为总尝试次数；代码里 `表现置信度 = max(0, L) × 50`。

**为什么是亮点**：

- 新生只做 1 题且对了，朴素准确率 p=100%，但样本太小不可信——Wilson 下界 `L` 会显著低于 1.0（小样本"打折"），表现置信度随之压低。这是对小样本偏差的标准统计修正（Wilson, 1927, *JASA*）。
- `样本充足度 = ln(1+n)/ln(1+50)×50` 是对数增长——前几次练习置信度涨得快，之后边际递减，符合"少量数据也能用、但别过度自信"的直觉。

> **澄清（避免误读）**：Wilson 下界**已被真实用于** `compute_confidence` 的"表现置信度"项（非摆设）。但 `confidence_score` 目前**只写入画像、尚未反馈到排序阶段**——即"算出来了但还没用于门控"，详见改进方向 8。"Wilson 是否参与计算"与"置信度是否门控排序"是两件不同的事，请勿混淆。

---

### 亮点 5：可解释推荐（每条推荐带自然语言理由）

**是什么**（`ranking.py:335-414`）：每条推荐生成 `reason_text`，能讲清三件事——①考什么知识点（主标签+相关度）；②为什么推给你（你在这个知识点掌握度多少、练过几次）；③难度是否合适。

**为什么是亮点**：推荐系统的"可解释性"是教研场景的刚需（老师/学生要知道"为什么推这题"），很多推荐系统只给分不给理由。本系统把排序中间量（need_match、掌握度、难度）直接转成中文句子，**答辩时可直接演示**。

---

### 亮点 6：全链路优雅降级（鲁棒性突出）

**是什么**：每一层、每一路都有 try/except + 空返回兜底：

- 语义召回（`recall.py:180-242`）：sentence-transformers 没装 → 表没数据 → 模型 OOM → 全部返回 `[]`，主流程继续走 A+B。
- 召回合并（`recall.py:245-331`）：任何一路失败不影响其他路，最后用热门题兜底补足。
- 画像缺失（`recommendation_service.py:196-222`）：`_fallback_recommendations` 直接返回质量分排序的题。

**为什么是亮点**：既有评审（`AUTO_REVIEW.md`）专门验证过这一点——DeepSeek 怀疑的 5 个崩溃点，3 个是误报（代码已有保护）。**服务永远不会因为单点故障整体崩溃**，这是生产可用的素质。

---

### 亮点 7：错题本双信号加权（未掌握强 boost / 已掌握弱 boost）

**是什么**（`wrong_question_features.py:55-82`）：`boost = min(1.0, unresolved_hits + 0.3×resolved_hits)/3`。

**为什么是亮点**：区分了"还没掌握的错题 tag"（强 boost 1.0）和"已掌握但相关"的 tag（弱 boost 0.3，保持相关练习在轮换里）。这比"错题 tag 一律加权"更精细，避免了"已掌握的题反复推"。

---

## 三、改进方向（基础模型不变，针对本项目优化）

> 以下 10 条按 **"性价比 = 影响面 ÷ 落地难度"** 从高到低排序。每条给出：现状 → 问题 → 方案 → 预期收益。**均不改动 BKT/艾宾浩斯/TF-IDF/Embedding 的核心数学**，只在其外围做组合优化、参数优化、特征补全。

---

### 改进 1（高优先级）：`P_success` 没用上 BKT 的观测模型

**现状**（`ranking.py:129-155`）：通过概率是掌握度的**线性映射** `min(0.95, avg_mastery×0.85 + 0.15)`。

**问题**：BKT 本身就自带观测模型 `P(correct) = P(L)·(1−P(S)) + (1−P(L))·P(G)`，能从掌握度推出"做对这道题的概率"。当前代码里 `P(G)`、`P(S)` 只在画像更新时用了，**在通过概率预测时却弃之不用**，等于浪费了 BKT 的一半能力。

**方案**：把 `compute_success_prob` 改为用 BKT 观测模型——取题目的相关 tag 掌握度作为 `P(L)`，代入 `P(L)·(1−slip) + (1−P(L))·guess`。`slip`/`guess` 可按题目难度档位微调（Easy 的 guess 高、Hard 的 slip 高）。

**预期收益**：通过概率预测从"经验线性公式"升级为"模型推导值"，且能在题目难度上体现差异（同样掌握度下，难题通过率更低）——**答辩时这是一个"用足了基础模型"的加分点**。

**落地难度**：低（纯函数替换，~20 行）。

---

### 改进 2（高优先级）：召回阶段排序与排序阶段 `need` 函数不一致

**现状**（`recall.py:107-125`）：弱项召回**只按 `mastery_score` 升序**取最低 5 个 tag。

**问题**：排序阶段的 `need(tag)` 融合了三信号 `0.75·(1−m) + 0.10·f + 0.15·w_course`（`ranking.py:53-66`），但召回阶段却只看掌握度一个信号。结果：一个"掌握度中等但遗忘度极高"的 tag，或"掌握度低且课程权重大"的核心 tag，可能在召回环节就被漏掉，排序阶段再精细也救不回来——**召回是排序的天花板**。

**方案**：召回时直接复用 `compute_need()` 给每个 tag 打分，取 need 最高的 Top-5 进入召回。

**预期收益**：召回候选与排序目标对齐，减少"该练的没召回进来"。**零成本的对齐修复**。

**落地难度**：低（~5 行）。

---

### 改进 3（高优先级）：`MMR` 是约束式贪心，不是真正的 MMR

**现状**（`ranking.py:273-332`）：当前"多样性重排"只有两条硬约束——①单标签占比 ≤40%；②相邻 2 题不能完全同标签。靠两轮贪心选取。

**问题**：真正的 MMR（Maximal Marginal Relevance）公式是 `argmax [ λ·Sim(item, query) − (1−λ)·max_{s∈已选} Sim(item, s) ]`，在"相关性"和"多样性"之间用 λ 做连续权衡。当前实现是**约束满足**，不是**目标优化**，会出现"为了凑多样性把高分题挤掉、却又塞进低分题"的情况。

**方案**：升级为基于 tag 集合 Jaccard 重叠度的真正 MMR——每轮从候选里选 `λ·score − (1−λ)·与已选题的最大tag重叠度` 最大的题。

- **λ 的含义**：λ 控制 相关性-多样性 权衡——λ=1 退化为纯相关性排序，λ=0 退化为纯多样性；
- **取 0.7 的依据**：MMR 原文（Carbonell & Goldstein, 1998）及工业实践（新闻/商品重排）通常取偏相关性的 0.5~0.8，0.7 为该区间中点；上线后可用 A/B 测试校准；
- **tag 重叠度**用 Jaccard：`|tags(item) ∩ tags(已选)| / |tags(item) ∪ tags(已选)|`，取与所有已选题的最大值作多样性惩罚项。

**预期收益**：推荐列表的"既相关又不重复"在数学上有最优解依据，比硬约束更平滑。**答辩可讲清 MMR 的来历**。

**落地难度**：中（重写 `diversity_rerank`，~40 行，需配测试）。

---

### 改进 4（高优先级）：排序权重未归一化，`score` 缺乏概率解释

**现状**：正项权重之和 `0.40+0.20+0.15+0.10+0.10+0.10 = 1.05`（`config.py:21-27`），各因子取满 1 时 `score` 会 >1，靠 `clamp(0, 1.2)`（`ranking.py:250`）硬截断。另外 **README 文档值与 config 实际值不一致**，具体差异：

| 因子 | README 文档值 | config.py 实际值 |
|------|--------------|-----------------|
| need_match | **0.45** | **0.40** |
| difficulty_fit / success_prob / novelty / quality / wrong_question | 0.20 / 0.15 / 0.10 / 0.10 / 0.10 | 同左（一致） |

即唯一偏差是 `need_match`（文档 0.45、代码 0.40），导致正项之和文档侧 1.10、代码侧 1.05。

**问题**：

1. `score` 没有归一化语义，0.85 和 0.72 的差距无法解释为"相关性高多少"；
2. 截断到 1.2 会把头部题的区分度压平（多个题都顶到 1.2）；
3. 文档与实现不一致是答辩硬伤。

**方案**：①把正项权重归一化为和=1（如除以 1.05）；②`score` 输出再做一次分位数校准或 `score/1.05`，落在 [0,1]；③同步修正 README 与 config。

**预期收益**：分数有概率/相关度语义，排序区分度提升，文档与代码一致。

**落地难度**：低。

---

### 改进 5（高优先级）：`quality_score` / `estimated_minutes` 目前是默认值，排序质量因子形同虚设

**现状**：核对爬虫 `models.go` 与 `schema.sql:14-16`，`quality_score` 默认 `0.7000`、`estimated_minutes` 默认 `30`，**爬虫并没有抓取真实值**。于是排序里的 `quality` 因子（权重 0.10）对所有题基本是常数，没有区分度。

**问题**：六因子里有一因子（10%权重）实际失效，等于"名义六因子、实际五因子"。

**方案**（基础模型不变，补数据）：
- 扩展爬虫抓 LeetCode 的 `acRate`（通过率）作为 `quality_score` 的真实来源；
- `estimated_minutes` 可用 `难度档位 × 经验系数`（Easy 15 / Medium 30 / Hard 50）离线估算写入。

**预期收益**：质量因子真正生效，热门兜底和质量排序也有了真实依据。

**落地难度**：中（需改 Go 爬虫 + 重新跑数据）。

---

### 改进 6（中优先级）：难度只有三档离散值，`d_target` 线性映射粗糙

**现状**（`ranking.py:118-126`）：`d_target = (avg_mastery/100)×2 + 1`，把掌握度线性映射到 Easy=1/Medium=2/Hard=3；题目难度也只有这三档。

**问题**：难度是**离散三值**，"刚过 Medium 门槛的 Hard"和"最难的 Hard"无法区分；`d_target` 线性映射对掌握度 50~70 这段（最常见区间）都落在 Medium，区分度低。

**方案**（基础模型不变，增特征）：
- 引入**连续难度** `difficulty_cont = f(acRate, 档位)`（需改进 5 先补 acRate）；
- `d_target` 改为掌握度的**分段/对数映射**，并在 `difficulty_fit` 里用连续难度计算 `e^(-|Δ|)`。
- **可行性边界**：连续难度依赖改进 5 先补 `acRate`；在当前"仅三档难度 + quality_score 默认 0.7"的约束下，若短期拿不到 acRate，可退化为"档位 × 学生历史通过率"的近似连续值——基础模型 `difficulty_fit` 的高斯衰减结构不变，只是 `d_problem` 从离散 {1,2,3} 变为连续。

**预期收益**：难度匹配从"三档跳变"变"连续平滑"，靠近学生当前能力边沿的题（最近发展区）更容易被选出。

**落地难度**：中（依赖改进 5 的数据）。

---

### 改进 7（中优先级）：BKT 参数全局固定，未做个性化/分难度

**现状**（`config.py:30-33`）：`P(T)=0.14, P(G)=0.20, P(S)=0.10, P(L0)=0.30` 对**所有学生、所有知识点**都一样。

**问题**：DP 和"数组"的猜对率显然不同（数组题容易蒙，DP 很难蒙）；强生和弱生的学习转移率 `P(T)` 也不同。固定参数会让 BKT 在部分 tag 上失准。

**方案**（基础模型不变，参数层优化）：
- **轻量版**：按 tag 的 `course_weight` 或难度档位，给 `P(G)/P(S)` 设分桶默认值（如 Hard 的 `P(G)↓` `P(S)↑`）；
- **进阶版**：用 EM 算法从历史练习数据离线拟合每个 tag 的四参数（BKT 标准做法，Corbett & Anderson 1995 原文支持参数估计）——**E 步**用前向-后向（forward-backward）算法，在当前参数下估计每个学生每次练习后处于"已掌握"隐状态的后验概率；**M 步**用这些期望频次重估 `P(T)/P(G)/P(S)/P(L0)`；迭代至收敛。每个 tag 独立拟合，得到 tag 级参数表。

**预期收益**：掌握度估计更贴合具体知识点，画像更准。

**落地难度**：轻量版低；进阶版中高（需离线训练脚本）。

---

### 改进 8（中优先级）：Wilson 置信度算了但没充分用（门控缺失）

**现状**：`confidence_score` 已写入画像（`unified_state.py:101`），但排序阶段**完全没有读取/使用它**。

**问题**：新生只练 1 次的掌握度置信度极低，却和练了 50 次的掌握度同等权重参与排序，导致冷启动期推荐噪声大。

**方案**：在 `compute_need_match` / `compute_success_prob` 里给掌握度乘一个**置信度门控** `w_conf = confidence/100`（或更平滑的 sigmoid），置信度低的 tag 掌握度信号被弱化、探索信号相对增强。

**预期收益**：把已算好的置信度用起来，冷启动期推荐更稳健。**把亮点 4 从"算了"升级为"用上了"**。

**落地难度**：低。

---

### 改进 9（中优先级）：缺少知识点前置关系（prerequisite DAG）

**现状**：推荐完全按"弱项"独立选题，不考虑知识点间的**学习先后依赖**。

**问题**：学生"动态规划"弱，但可能是因为"递归/分治"没掌握；直接推 DP 题不如先推递归。当前系统推不出这种"先补基础再攻难点"的策略。

**方案**（基础模型不变，加一层规则/图）：
- 离线维护一张小规模知识点**前置关系 DAG**（递归→分治→DP；数组→双指针→滑动窗口；树→BST→堆…，几十条人工边即可）；
- 排序时给"当前弱项 tag 的前置 tag 未掌握"的题降权，或新增一路"前置补强召回"。

**预期收益**：推荐符合教学逻辑（最近发展区 + 先修关系），教研价值显著。**答辩亮点级功能**。

**落地难度**：中（需人工整理 DAG，~50 条边；排序接入 ~30 行）。

---

### 改进 10（中优先级）：探索/利用固定配额，探索 tag 硬编码

**现状**（`recall.py:39, 144-153`）：探索召回固定 15% 配额，且只在**硬编码**的 `_EXPLORATION_TAGS = {动态规划, 贪心, 回溯, 图, 树, 堆, 并查集, 位运算}` 里探索。

**问题**：
1. 探索 tag 写死，没覆盖全部 54 知识点；
2. 配额固定，不随学生"探索充分度"动态变化（已经探索很多的学生还按 15% 探索，浪费；新学生又可能探索不够）；
3. 缺乏 EE（探索-利用）的理论依据。

**方案**：
- 探索 tag 改为**动态计算**：取学生画像里"练习次数<N"的所有 tag（不限于硬编码集合）；
- 配额用 **ε-greedy 或 UCB1** 动态调整：
  - **ε-greedy**：以概率 ε 随机探索未充分练习的 tag、否则按弱项利用；ε 随学生总练习量衰减（如 `ε = ε₀/(1+t/T)`）；
  - **UCB1**：选 `argmax [ mastery(tag) + c·√(ln N / n_tag) ]`，`N` 为学生总曝光数、`n_tag` 为该 tag 历史曝光数——曝光少的 tag 探索项自动增大，无需固定 15% 配额。

**预期收益**：探索覆盖更全、配额更合理，有 EE 理论背书。

**落地难度**：低-中。

---

## 四、改进优先级矩阵

| # | 改进项 | 影响 | 难度 | 推荐批次 | 是否需改爬虫/数据 |
|---|--------|------|------|----------|------------------|
| 2 | 召回与 need 对齐 | 高 | 低 | **第一批**（快赢） | 否 |
| 4 | 权重归一化 + 文档对齐 | 中 | 低 | **第一批**（快赢） | 否 |
| 1 | P_success 用 BKT 观测模型 | 高 | 低 | **第一批**（快赢） | 否 |
| 8 | 置信度门控 | 中 | 低 | **第一批**（快赢） | 否 |
| 3 | 真 MMR 重排 | 高 | 中 | 第二批 | 否 |
| 7 | BKT 参数分桶/拟合 | 中 | 低-中 | 第二批 | 否（轻量版） |
| 10 | 动态探索/EE | 中 | 低-中 | 第二批 | 否 |
| 9 | 知识点前置 DAG | 高 | 中 | 第三批（亮点功能） | 否（人工整理） |
| 5 | 补 quality_score/耗时 | 高 | 中 | 第四批 | **是**（改爬虫） |
| 6 | 连续难度 | 中 | 中 | 第四批 | **是**（依赖 5） |

> **建议节奏**：第一批（1/2/4/8）全是不改数据、不改爬虫的纯算法层快赢，1~2 天可落地，立刻让"用足基础模型"的故事完整；第二批做架构级升级（真 MMR、参数个性化、EE）；第三批的前置关系 DAG 是答辩杀手锏；第四批补数据需要联动 Go 爬虫，放最后。

---

## 五、结论

**这套推荐算法有真亮点，不是"拼凑的学生项目"**。最值得讲的三件事是：

1. **`P(recall)=P(L)×R(t)` 的非累积衰减**（亮点 1）——数学上自洽的 KT+SR 融合，是全系统的技术制高点；
2. **三层混合匹配**（亮点 2）——强/中/弱信号分层兜底的工业匹配架构；
3. **完整召回-排序-MMR-可解释流水线**（亮点 3/5）——架构完整且鲁棒（亮点 6）。

**改进空间集中在"基础模型已经在了，但没用满、没对齐"**：

- 最该先做的 4 件事（改进 1/2/4/8）都是**不改模型、不改数据**的纯优化，性价比极高，且能让答辩叙事从"实现了 BKT"升级为"**用透了 BKT**"；
- 真正能让项目"上一个台阶"的是**改进 9（知识点前置 DAG）**——它把推荐从"按弱项选题"提升为"按学习路径选题"，教研价值直接拉满，是少数能形成差异化竞争力的方向；
- 数据侧（改进 5/6）需要联动爬虫，建议作为长期项。

> 一句话总结：**亮点扎实（尤其统一状态建模），改进方向清晰且可分批落地；优先把"已有模型用满"做完，再补"知识点关系图"做差异化。**
